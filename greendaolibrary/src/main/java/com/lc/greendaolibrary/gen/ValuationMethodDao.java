package com.lc.greendaolibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.lc.greendaolibrary.dao.ValuationMethod;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "VALUATION_METHOD".
*/
public class ValuationMethodDao extends AbstractDao<ValuationMethod, Long> {

    public static final String TABLENAME = "VALUATION_METHOD";

    /**
     * Properties of entity ValuationMethod.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Value = new Property(2, String.class, "value", false, "VALUE");
        public final static Property Sort = new Property(3, int.class, "sort", false, "SORT");
        public final static Property Is_default = new Property(4, boolean.class, "is_default", false, "IS_DEFAULT");
    }


    public ValuationMethodDao(DaoConfig config) {
        super(config);
    }
    
    public ValuationMethodDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"VALUATION_METHOD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"VALUE\" TEXT UNIQUE ," + // 2: value
                "\"SORT\" INTEGER NOT NULL ," + // 3: sort
                "\"IS_DEFAULT\" INTEGER NOT NULL );"); // 4: is_default
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_VALUATION_METHOD_SORT ON \"VALUATION_METHOD\"" +
                " (\"SORT\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"VALUATION_METHOD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ValuationMethod entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String value = entity.getValue();
        if (value != null) {
            stmt.bindString(3, value);
        }
        stmt.bindLong(4, entity.getSort());
        stmt.bindLong(5, entity.getIs_default() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ValuationMethod entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String value = entity.getValue();
        if (value != null) {
            stmt.bindString(3, value);
        }
        stmt.bindLong(4, entity.getSort());
        stmt.bindLong(5, entity.getIs_default() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ValuationMethod readEntity(Cursor cursor, int offset) {
        ValuationMethod entity = new ValuationMethod( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // value
            cursor.getInt(offset + 3), // sort
            cursor.getShort(offset + 4) != 0 // is_default
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ValuationMethod entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setValue(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSort(cursor.getInt(offset + 3));
        entity.setIs_default(cursor.getShort(offset + 4) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ValuationMethod entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ValuationMethod entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ValuationMethod entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
