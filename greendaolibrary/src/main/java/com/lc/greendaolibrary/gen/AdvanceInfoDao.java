package com.lc.greendaolibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.lc.greendaolibrary.dao.AdvanceInfo;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ADVANCE_INFO".
*/
public class AdvanceInfoDao extends AbstractDao<AdvanceInfo, Long> {

    public static final String TABLENAME = "ADVANCE_INFO";

    /**
     * Properties of entity AdvanceInfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property IsChangeable = new Property(2, boolean.class, "isChangeable", false, "IS_CHANGEABLE");
        public final static Property IsDirect = new Property(3, boolean.class, "isDirect", false, "IS_DIRECT");
        public final static Property IsMoreMethods = new Property(4, boolean.class, "isMoreMethods", false, "IS_MORE_METHODS");
    }


    public AdvanceInfoDao(DaoConfig config) {
        super(config);
    }
    
    public AdvanceInfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ADVANCE_INFO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"NAME\" TEXT UNIQUE ," + // 1: name
                "\"IS_CHANGEABLE\" INTEGER NOT NULL ," + // 2: isChangeable
                "\"IS_DIRECT\" INTEGER NOT NULL ," + // 3: isDirect
                "\"IS_MORE_METHODS\" INTEGER NOT NULL );"); // 4: isMoreMethods
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ADVANCE_INFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AdvanceInfo entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
        stmt.bindLong(3, entity.getIsChangeable() ? 1L: 0L);
        stmt.bindLong(4, entity.getIsDirect() ? 1L: 0L);
        stmt.bindLong(5, entity.getIsMoreMethods() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AdvanceInfo entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
        stmt.bindLong(3, entity.getIsChangeable() ? 1L: 0L);
        stmt.bindLong(4, entity.getIsDirect() ? 1L: 0L);
        stmt.bindLong(5, entity.getIsMoreMethods() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AdvanceInfo readEntity(Cursor cursor, int offset) {
        AdvanceInfo entity = new AdvanceInfo( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.getShort(offset + 2) != 0, // isChangeable
            cursor.getShort(offset + 3) != 0, // isDirect
            cursor.getShort(offset + 4) != 0 // isMoreMethods
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AdvanceInfo entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIsChangeable(cursor.getShort(offset + 2) != 0);
        entity.setIsDirect(cursor.getShort(offset + 3) != 0);
        entity.setIsMoreMethods(cursor.getShort(offset + 4) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AdvanceInfo entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AdvanceInfo entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AdvanceInfo entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
