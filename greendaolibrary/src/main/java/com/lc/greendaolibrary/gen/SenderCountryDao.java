package com.lc.greendaolibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.lc.greendaolibrary.dao.SenderCountry;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SENDER_COUNTRY".
*/
public class SenderCountryDao extends AbstractDao<SenderCountry, Long> {

    public static final String TABLENAME = "SENDER_COUNTRY";

    /**
     * Properties of entity SenderCountry.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CnName = new Property(1, String.class, "cnName", false, "CN_NAME");
        public final static Property DefaultValue = new Property(2, String.class, "defaultValue", false, "DEFAULT_VALUE");
        public final static Property BarCodeNumber = new Property(3, String.class, "barCodeNumber", false, "BAR_CODE_NUMBER");
    }


    public SenderCountryDao(DaoConfig config) {
        super(config);
    }
    
    public SenderCountryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SENDER_COUNTRY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CN_NAME\" TEXT UNIQUE ," + // 1: cnName
                "\"DEFAULT_VALUE\" TEXT," + // 2: defaultValue
                "\"BAR_CODE_NUMBER\" TEXT);"); // 3: barCodeNumber
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SENDER_COUNTRY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, SenderCountry entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String cnName = entity.getCnName();
        if (cnName != null) {
            stmt.bindString(2, cnName);
        }
 
        String defaultValue = entity.getDefaultValue();
        if (defaultValue != null) {
            stmt.bindString(3, defaultValue);
        }
 
        String barCodeNumber = entity.getBarCodeNumber();
        if (barCodeNumber != null) {
            stmt.bindString(4, barCodeNumber);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, SenderCountry entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String cnName = entity.getCnName();
        if (cnName != null) {
            stmt.bindString(2, cnName);
        }
 
        String defaultValue = entity.getDefaultValue();
        if (defaultValue != null) {
            stmt.bindString(3, defaultValue);
        }
 
        String barCodeNumber = entity.getBarCodeNumber();
        if (barCodeNumber != null) {
            stmt.bindString(4, barCodeNumber);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public SenderCountry readEntity(Cursor cursor, int offset) {
        SenderCountry entity = new SenderCountry( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // cnName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // defaultValue
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // barCodeNumber
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, SenderCountry entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCnName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDefaultValue(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setBarCodeNumber(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(SenderCountry entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(SenderCountry entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(SenderCountry entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
