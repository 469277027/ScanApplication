package com.lc.greendaolibrary.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.lc.greendaolibrary.dao.ReceiveCountry;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RECEIVE_COUNTRY".
*/
public class ReceiveCountryDao extends AbstractDao<ReceiveCountry, Long> {

    public static final String TABLENAME = "RECEIVE_COUNTRY";

    /**
     * Properties of entity ReceiveCountry.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CnName = new Property(1, String.class, "cnName", false, "CN_NAME");
        public final static Property PId = new Property(2, String.class, "pId", false, "P_ID");
        public final static Property BarCodeNumber = new Property(3, String.class, "barCodeNumber", false, "BAR_CODE_NUMBER");
    }


    public ReceiveCountryDao(DaoConfig config) {
        super(config);
    }
    
    public ReceiveCountryDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RECEIVE_COUNTRY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CN_NAME\" TEXT UNIQUE ," + // 1: cnName
                "\"P_ID\" TEXT," + // 2: pId
                "\"BAR_CODE_NUMBER\" TEXT);"); // 3: barCodeNumber
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RECEIVE_COUNTRY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ReceiveCountry entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String cnName = entity.getCnName();
        if (cnName != null) {
            stmt.bindString(2, cnName);
        }
 
        String pId = entity.getPId();
        if (pId != null) {
            stmt.bindString(3, pId);
        }
 
        String barCodeNumber = entity.getBarCodeNumber();
        if (barCodeNumber != null) {
            stmt.bindString(4, barCodeNumber);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ReceiveCountry entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String cnName = entity.getCnName();
        if (cnName != null) {
            stmt.bindString(2, cnName);
        }
 
        String pId = entity.getPId();
        if (pId != null) {
            stmt.bindString(3, pId);
        }
 
        String barCodeNumber = entity.getBarCodeNumber();
        if (barCodeNumber != null) {
            stmt.bindString(4, barCodeNumber);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ReceiveCountry readEntity(Cursor cursor, int offset) {
        ReceiveCountry entity = new ReceiveCountry( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // cnName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // pId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // barCodeNumber
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ReceiveCountry entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCnName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setPId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setBarCodeNumber(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ReceiveCountry entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ReceiveCountry entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ReceiveCountry entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
